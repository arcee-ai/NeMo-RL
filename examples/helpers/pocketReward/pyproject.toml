[project]
name = "pocketReward"
version = "0.1.0"
description = "Reward client wrapper"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["pocketReward.py"]
